// Bundled into static/haystack/haystack-sdk.bundle.js as the `HaystackRouter`
// global. Build: see package.json `build:sdk`. Re-exports only what the shared
// swap controller needs from the Haystack SDK.
export { RouterClient } from "@txnlab/haystack-router";
